{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "typecheck": { "dependsOn": ["^typecheck"], "inputs": ["src/**", "test/**", "tsconfig.json", "../../tsconfig.base.json"], "outputs": [] }
  }
}
